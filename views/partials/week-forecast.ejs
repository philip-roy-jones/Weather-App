<div class="card-content">
    <h2>3-Hour Forecast</h2>
    <ul>
        <% weekData.list.forEach(item => { %>
            <% const localTime = new Date(item.dt * 1000); %>
            <li>
                <strong><%= localTime.toLocaleString() %></strong>: <%= Math.round(item.main.temp) %>°, <%= item.weather[0].description %>
            </li>
        <% }) %>
    </ul>
</div>